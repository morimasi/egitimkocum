// This file is a placeholder to prevent Vercel build errors.
// It creates a valid, but unused, serverless function.
export default (req, res) => {
  res.status(200).send('This function is not in use.');
};
